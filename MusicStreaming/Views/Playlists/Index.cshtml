@model List<MusicStreaming.Models.Playlist>

@{
    ViewData["Title"] = "My Playlists";
}

@section Scripts {
    <script>
        const savedPlaylistIds = new Set([
            @(string.Join(",", (ViewData["SavedPlaylistIds"] as HashSet<int> ?? new HashSet<int>()).Select(x => x.ToString())))
        ]);
    </script>
}

<h1>My Playlists</h1>

<a href="@Url.Action("Create", "Playlists")" class="btn btn-primary" style="margin-bottom: 1.5rem;">Create New Playlist</a>

@if (Model?.Count > 0)
{
    <table class="table">
        <thead>
            <tr>
                <th>Playlist Name</th>
                <th>Description</th>
                <th>Songs</th>
                <th>Created</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var playlist in Model)
            {
                var isSaved = ((ViewData["SavedPlaylistIds"] as HashSet<int>)?.Contains(playlist.Id)) ?? false;
                <tr>
                    <td>
                        <a href="@Url.Action("Details", "Playlists", new { id = playlist.Id })">
                            @playlist.PlaylistName
                        </a>
                    </td>
                    <td>@playlist.Description</td>
                    <td>@playlist.PlaylistSongs.Count</td>
                    <td>@playlist.CreatedAt.ToString("MMM dd, yyyy")</td>
                    <td>
                        <button class="btn btn-heart btn-sm @(isSaved ? "saved" : "")"
                                type="button"
                                onclick="toggleSavePlaylist(@playlist.Id, this)">
                            @(isSaved ? "♥ Saved" : "♡ Save")
                        </button>
                        <a href="@Url.Action("Edit", "Playlists", new { id = playlist.Id })" class="btn btn-info btn-sm">Edit</a>
                        <a href="@Url.Action("Delete", "Playlists", new { id = playlist.Id })" class="btn btn-danger btn-sm">Delete</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
}
else
{
    <p>No playlists yet. <a href="@Url.Action("Create", "Playlists")">Create one</a></p>
}
